#!/usr/bin/env python3
"""file to convert a number to a string
"""


def to_str(n: float) -> str:
    """function that converts a real number to a string

    Args:
        n (float): real number that is to be converted

    Returns:
        str: resulting string
    """
    return str(n)
